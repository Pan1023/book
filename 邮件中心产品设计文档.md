# 邮件中心（邮件发送与追踪模块）产品设计文档

> 版本：v1.0 ｜ 日期：2026-09-03
> 文档定位：整体方向与主要功能梳理，详细交互与接口设计在后续详细设计阶段补充。

---

## 1. 产品概述

### 1.1 产品定位

邮件中心是面向销售、市场、客服等团队的**一站式邮件管理与追踪平台**，覆盖从"单点邮件往来"到"批量邮件任务投放与效果分析"的完整链路，帮助用户：

- **管得住**：多个自有邮箱统一绑定、统一收发；
- **发得出**：单发、批量发均稳定可靠，失败可重试；
- **看得见**：发送结果、客户反馈、关键指标可视化；
- **找得到**：从大量互动中自动识别高意向客户并导出跟进。

### 1.2 目标用户

| 用户角色 | 核心诉求 |
| --- | --- |
| 销售人员 | 用自己的邮箱一对一联系客户，追踪谁看了、谁回了，筛选意向客户 |
| 市场/运营人员 | 批量发起邮件任务（如产品推广、活动通知），查看送达率/打开率/回复率 |
| 客服/商务人员 | 统一管理多个邮箱的来信，按联系人归档会话历史 |
| 管理员 | 配置邮箱通道、查看系统报表、管控数据安全与权限 |

### 1.3 核心价值

1. **多邮箱统一入口**：企业邮箱、Gmail 等主流邮箱一次绑定，收发集中处理；
2. **发送与追踪闭环**：发邮件 → 追踪状态（送达/打开/回复/退信）→ 数据统计 → 意向客户沉淀；
3. **数据驱动销售**：用互动数据替代人工判断，自动标记高意向客户，提升转化效率。

---

## 2. 产品整体架构

系统分为两大业务模块 + 一个基础支撑层：

```
┌─────────────────────────────────────────────────────────┐
│                    用户端（Web 响应式界面）                │
├──────────────────────────┬──────────────────────────────┤
│   模块一：普邮管理         │   模块二：邮件追踪分析         │
│  ┌────────────────────┐  │  ┌────────────────────────┐  │
│  │ · 邮箱绑定管理      │  │  │ · 邮件任务管理（批量）  │  │
│  │ · 一对一邮件发送    │  │  │ · 多任务合并统计报表    │  │
│  │ · 邮件编辑器        │  │  │ · 客户反馈汇总          │  │
│  │ · 会话历史归档      │  │  │ · 可视化数据看板        │  │
│  │ · 统一收件箱        │  │  │ · 意向客户提取与导出    │  │
│  └────────────────────┘  │  └────────────────────────┘  │
├──────────────────────────┴──────────────────────────────┤
│              基础支撑层（通道 / 安全 / 集成）              │
│  SMTP/IMAP 通道 · 发送队列与重试 · 数据加密存储 ·         │
│  追踪埋点（像素/链接）· 开放 API · 权限体系               │
└─────────────────────────────────────────────────────────┘
```

---

## 3. 核心功能设计

### 模块一：普邮管理（点对点邮件）

#### 3.1 邮箱绑定与配置

- 支持绑定用户**自有邮箱**：企业邮箱（自有域名）、Gmail 等主流服务商；
- 配置方式：SMTP（发信）+ IMAP（收信）服务器参数，提供主流邮箱预设模板，自动填充服务器配置；
- 支持多邮箱绑定，可设置默认发信账号、签名档；
- 凭据加密存储，连接状态实时检测（正常/失效提醒）。

#### 3.2 一对一邮件发送

- **邮件编辑器**：富文本编辑、附件上传、邮件模板选择与插入；
- 支持发件人选择（已绑定邮箱）、抄送/密送、定时发送；
- 发送状态反馈：发送中 / 成功 / 失败（附失败原因）。

#### 3.3 会话历史归档

- 按**联系人**自动聚合往来邮件，形成会话视图（类似邮件聊天串）；
- 完整保存历史记录，支持按联系人检索、查看时间线；
- 联系人与邮件任务中的收件人数据互通。

#### 3.4 统一收件箱

- 所有绑定邮箱的来信**集中展示**于一个收件箱；
- 支持按时间、联系人、已读/未读、所属邮箱账号等维度筛选；
- 来信可直接回复，回复自动归入对应联系人会话。

---

### 模块二：邮件追踪分析

#### 3.5 邮件任务管理（批量发送）

- 创建邮件任务：批量导入收件人（CSV/Excel 或手动录入）、设置邮件内容（可套用模板）、设置发送时间（立即/定时）；
- 任务列表：查看任务状态（待发送/发送中/已完成/失败）、发送进度、基础统计；
- 支持任务的暂停、复制、重新发送失败地址。

#### 3.6 多任务合并统计

- 支持跨多个邮件任务**合并维度统计**（如按时间段、按客户分组、按模板分组）；
- 生成综合分析报表，支持导出。

#### 3.7 客户反馈汇总

- 自动收集并分类收件方状态：
  - **送达** / **退信（硬退/软退）**
  - **已打开**（追踪像素）/ **未读**
  - **已回复** / 链接点击
- 反馈数据回流到对应收件人与任务。

#### 3.8 可视化数据看板

- 核心指标：**送达率、打开率、回复率、退信率**；
- 图表形式：趋势图（随时间变化）、任务对比图、漏斗图（发送→送达→打开→回复）；
- 支持按任务、时间范围、邮箱账号筛选查看。

#### 3.9 意向客户提取

- **智能标记规则**（基于可配置的评分模型）：
  - 回复速度（越快意向越高）；
  - 回复内容关键词（如"报价""试用""合作"等正向词）；
  - 互动频率（多次打开、多次回复、点击链接）；
- 输出高意向客户名单，支持**一键筛选**；
- **导出**：CSV / Excel，内容包含客户基本信息 + 互动历史摘要。

---

## 4. 关键业务流程（概要）

**流程一：单发邮件**
绑定邮箱 → 打开编辑器（选模板/传附件）→ 发送 → 队列投递（失败自动重试）→ 对方回复进入统一收件箱 → 按联系人归档会话。

**流程二：批量任务追踪**
导入收件人 → 创建任务（内容/时间）→ 定时批量发送（带追踪埋点）→ 系统回收打开/点击/回复/退信数据 → 看板统计 + 多任务合并报表 → 评分模型标记意向客户 → 筛选导出跟进。

---

## 5. 非功能需求（技术要求）

| 类别 | 要求 |
| --- | --- |
| **稳定性** | 发送队列异步化；失败自动重试（可配置重试次数与间隔）；退信地址自动标记，避免重复发送 |
| **安全性** | 邮箱凭据与邮件内容加密存储；传输层加密（TLS/SSL）；操作权限管控与审计日志 |
| **易用性** | 现代 UI/UX 设计，Web 端响应式布局，适配桌面与平板 |
| **可集成性** | 提供完整开放 API（邮箱管理、发送、任务、统计数据、客户数据），支持与 CRM 等外部系统对接 |
| **可测试性** | 核心功能单元测试 + 集成测试，测试覆盖率不低于 80% |

---

## 6. 交付物清单

1. 功能完整的邮件中心系统源代码；
2. 用户操作手册（终端用户使用说明）；
3. 管理员配置指南（邮箱通道、权限、安全配置）；
4. 系统部署文档与环境要求说明；
5. 单元测试与集成测试报告（核心功能覆盖率 ≥ 80%）。

---

## 7. 建议实施分期（参考）

| 阶段 | 范围 | 目标 |
| --- | --- | --- |
| 一期 | 邮箱绑定、一对一收发、编辑器、会话归档、统一收件箱 | 跑通"普邮管理"基础链路 |
| 二期 | 批量邮件任务、发送队列与重试、追踪埋点、反馈汇总、数据看板 | 跑通"发送→追踪→统计"闭环 |
| 三期 | 意向客户评分模型、导出、多任务合并报表、开放 API | 数据沉淀与系统集成，支撑销售转化 |
